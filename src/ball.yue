{ graphics: lg } = love

export class Ball
  new: (x, y)=>
    @pos = { x: x, y: y }
    @speed = 10
    @image = lg.newImage "assets/sprites/blue-ball.png"

  draw: =>
    lg.draw(@image, @pos.x, @pos.y)

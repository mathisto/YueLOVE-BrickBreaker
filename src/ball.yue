{ graphics: lg } = love

export class Ball
  new: =>
    @pos = { x: 900, y: 850 }
    @speed = 10
    @image = lg.newImage "assets/sprites/blue-ball.png"

  -- update: (dt)=>

  draw: (dt, iTime)=>
    lg.draw(@image, @pos.x, @pos.y)
